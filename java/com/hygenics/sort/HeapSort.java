package com.hygenics.sort;

import java.util.ArrayList;

/**
 * Another large data algorithm, though merge sort with cosntant O(nlogn) at large ammounts of data is better.
 * Heap sort also has o(n) or o(nlogn) timing. The tree sort was put in place to offer general access to a tree
 * when I needed it, so stfu.
 * 
 * @author asevans
 *
 */
public class HeapSort<E>{
	
	private ArrayList<E> tosortlist;
	
	public HeapSort(){
		
	}
	
	
	
	private ArrayList<E> sort(){
		
		return tosortlist;
	}
	
	public ArrayList<E> run(){
		return sort();
	}

}
